# Surface the KV store collection as a lookup the dashboard can `| inputlookup`.
[counterspell_runbook]
external_type = kvstore
collection = counterspell_runbook
fields_list = _key, name, description, mitre, triage_steps, validation, spl
